PLAN PERFORM UpdateTaskCount() {
	ID: "http://www.arbi.com//UpdateTaskCount"
	PRECONDITION :
		FACT TaskTracker($tracker);
		FACT TaskManager($tm); 
	BODY:
		PERFORM isro:startLog("PERFORM", "UpdateTaskCount");
	
		$remainingTaskCount = $tracker.getPendingTasksCount();
		$remainingTaskContext = "(RemainingTaskCount " + $remainingTaskCount +")";
		$tm.assertFact($remainingTaskContext);
		
		$completeTaskCount = $tracker.getCompletedTasksCount();
		$completeTaskContext = "(CompletedTaskCount " + $completeTaskCount +")";
		$tm.assertFact($completeTaskContext);
		
		$ongoingTaskCount = $tracker.getOngoingTasksCount();
		$ongoingTaskContext = "(OngoingTaskCount " + $ongoingTaskCount +")";
		$tm.assertFact($ongoingTaskContext);
		
		$newTaskCount = $tracker.getNewTasksCount();
		$newTaskContext = "(NewTaskCount " + $newTaskCount +")";
		$tm.assertFact($newTaskContext);
		
		$averageSpeed = $tracker.getAverageSpeed();
		$averageTaskContext = "(AverageTaskSpeed " + $averageSpeed +")";
		$tm.assertFact($averageTaskContext);
		
		$cumulativeSpeed = $tracker.getCumulativeSpeed();
		$cumulativeTaskContext = "(CumulativeTaskSpeed " + $cumelativeSpeed +")";
		$tm.assertFact($cumulativeTaskContext);
		
		ASSERT UpdateTaskCount();
		
		PERFORM isro:endLog("PERFORM", "UpdateTaskCount");
	EFFECTS :
		RETRACT UpdateTaskCount();
	UTILITY: 10000; 
}